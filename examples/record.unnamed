let Nat = (A : _) -> Record { z : A, s : A -> A } -> A
  ; add : Nat -> Nat -> Nat = \x y A r. x _ r.{ z = y _ r }
  ; mul : Nat -> Nat -> Nat = \x y A r. x _ r.{ s = \(a : A). y _ r.{ z = a } }
  ; 2 : Nat = \A r. r.s (r.s r.z)
  ; 4 = add 2 2
  ; 16 = mul 4 4
  ; F = \(x : Nat). x _ { z = Record {}, s = \A. Record { 1 : Nat, 2 : A } }
  ; x : F 4 = { 1 = 2
        , 2 = { 1 = 2
        , 2 = { 1 = 4
        , 2 = { 1 = 16
        , 2 = {=} } } } }
 in x.2.2
